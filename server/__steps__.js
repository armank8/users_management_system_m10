/**
 * 1. Create a post api on the server side
 * 2. Client side send data via post
 * 3. set fetch method inside the fetch options
 *      (second parameter)
 * 4. Options will have 3 things : method
 * 5. Options will have headers: 'content-type':'application/json'
 * 6. Don't forget to send data by
 *      -> body:JSON.stringify(user)
 * 7. On the server side:
 *     Don't forget to use express.json() middleware;
 * 8. 
 *  
 */